def concat: [x for x in range(0, 10)] ++ [x for x in range(10, 20)]

print concat

class Array:
  def __concat__ x:
    x.each with index, element:
      self[index] = self[index] + element
    self

print concat
