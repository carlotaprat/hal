class Array:
  def filter:
    result = []
    for x in self: result.append! x if yield x
    return result

  def pop:
    r = []
    i = 1
    while i < size:
      r.append! self[i]
      i = i + 1
    return r

  def __add__ a:
    for x in a: append! x
    return self

  def sort:
    if size < 2: return self
    p = self[0]
    q = self.pop
    a = q.filter with x: x < p
    b = q.filter with x: x >= p
    print something
    a.sort + [p] + b.sort
